<UserControl x:Class="SmallQyest.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:SmallQyest.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewModels:MenuViewModel}">
    <Grid>
        <Button Command="{Binding NewGame}" Content="New Game" Width="300" Height="70" Margin="0,106,0,124" />
        <Button Command="{Binding Exit}" Content="Exit" Width="300" Height="70" Margin="0,190,0,40" />
    </Grid>
</UserControl>
